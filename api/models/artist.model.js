module.exports = class Artist {
	constructor(artist) {
		this.name = artist.name
		this.mbid = artist.mbid
		this.url = artist.url
		this.image_small = artist.image_small
		this.image = artist.imag
	}
}
